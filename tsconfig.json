{
  "compilerOptions": {
    "target": "es6", // Целевая версия JavaScript
    "module": "esnext", // Используем ES Modules
    "outDir": "./dist", // Директория для скомпилированных файлов
    "rootDir": "./src", // Корневая директория исходного кода
    "allowJs": false, // Запрещаем использование JavaScript
    "strict": true, // Включаем строгую проверку типов
    "esModuleInterop": true, // Включаем поддержку ES Modules
    "skipLibCheck": true, // Пропускаем проверку типов в библиотеках
    "resolveJsonModule": true, // Разрешаем импорт JSON
    "moduleResolution": "node", // Используем Node.js-стиль разрешения модулей
    "jsx": "react-jsx", // Используем React JSX
    "baseUrl": "./src", // Базовый путь для абсолютных импортов
    "paths": {
      "components/*": ["components/*"],
      "locales/*": ["locales/*"],
      "utils/*": ["utils/*"],
      "styles/*": ["styles/*"],
      "types/*": ["types/*"],
      "features/*": ["features/*"],
      "hooks/*": ["hooks/*"],
      "pages/*": ["pages/*"],
      "react-router-dom": ["node_modules/react-router-dom"]
    }
  },
  "include": [
    "src/**/*" // Включаем все файлы в директории src
, "src/__tests__/App.test.tsx"  ],
  "exclude": [
    "node_modules", // Исключаем node_modules
    "dist", // Исключаем директорию dist
    "**/*.test.js", // Исключаем тестовые файлы
    "**/*.js" // Исключаем JavaScript-файлы
  ]
}